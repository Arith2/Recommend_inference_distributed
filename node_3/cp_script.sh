# $1 -> dir of the new folder, e.g. ../new_folder
cp -r cp_script.sh cmake common fpga-network-stack host img kernel scripts .gitignore .gitmodules CMakeLists.txt config_hls.mk config_rtl.mk Makefile README.md utils.mk $1
